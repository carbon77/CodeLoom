info:
  name: Send submission
  type: http
  seq: 6

http:
  method: POST
  url: localhost:8081/v1/submissions
  body:
    type: json
    data: "{\r

      \  \"problemId\": 1,\r

      \  \"code\": \"print(sum(map(int, input().split())))\",\r

      \  \"language\": \"python\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
