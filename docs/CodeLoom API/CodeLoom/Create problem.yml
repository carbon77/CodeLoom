info:
  name: Create problem
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8081/v1/problems
  body:
    type: json
    data: |-
      {
          "title": "Two Sum"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
